# Railway (Nixpacks). ffmpeg viene de ffmpeg-static (npm) y yt-dlp de youtube-dl-exec (npm);
# python3 se instala por apt para que el yt-dlp (zipapp) lo encuentre en /usr/bin (sí está en PATH).
[phases.setup]
nixPkgs = ["nodejs_20"]
aptPkgs = ["python3"]

[start]
cmd = "node server.js"
